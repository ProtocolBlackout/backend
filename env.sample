MONGODB_URL=
DATABASE=
PORT=
JWT_SECRET=
JWT_EXPIRES_IN=

# SMTP (Fallback, nur wenn Gmail nicht vollständig konfiguriert ist)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# Gmail API (bevorzugt, wenn vollständig gesetzt)
GMAIL_CLIENT_ID=
GMAIL_CLIENT_SECRET=
GMAIL_REFRESH_TOKEN=
GMAIL_REDIRECT_URI=

# optional: Absender-Header; wenn leer, wird SMTP_FROM oder SMTP_USER verwendet
GMAIL_FROM=